package fr.isen.onlyfakes.enums

enum class Routes() {
    HOME,
    ADDPOST,
    ACCOUNT
}

enum class LoginRoutes() {
    LOGIN,
    REGISTER,
    RESET
}

enum class ProfileRoutes(){
    PAYEMENT,
    MODIFY
}